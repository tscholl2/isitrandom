Each `.shuffles` file contains the number of cards in each packet on either side of the shuffle.
The sum should equal 52, although there may be a few that are off because of poor counting (my own fault).
